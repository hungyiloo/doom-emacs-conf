;;; config/selectrum.el -*- lexical-binding: t; -*-

(after! selectrum
  (setq selectrum-max-window-height 12)
  ;; (setq selectrum-fix-vertical-window-height 24)
  ;; (setq selectrum-num-candidates-displayed 12)
  (setq selectrum-quick-keys '(?a ?r ?s ?t ?n ?e ?i ?o ?d ?h)))
