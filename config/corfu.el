;;; config/corfu.el -*- lexical-binding: t; -*-

;; (after! corfu
;;   (setq corfu-preselect-first t)

;;   (custom-set-faces!
;;     `(corfu-default :background ,(doom-color 'base1))
;;     `(corfu-current :background ,(doom-color 'base4)))

;;   (after! evil
;;     (map! :i "C-@" #'completion-at-point
;;           :i "C-SPC" #'completion-at-point
;;           (:prefix "C-x"
;;            :i "C-k" #'cape-dict
;;            :i "C-f" #'cape-file
;;            :i "s" #'cape-ispell
;;            :i "C-n" #'cape-keyword
;;            :i "C-s" #'cape-dabbrev
;;            :i "C-l" #'cape-line))))
