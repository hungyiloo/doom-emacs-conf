;;; config/pdf-tools.el -*- lexical-binding: t; -*-

(after! pdf-tools
  (custom-set-faces!
    `(pdf-isearch-match :foreground "#000" :background "#f7f")
    `(pdf-isearch-lazy :foreground "#000" :background "#fd0")))
